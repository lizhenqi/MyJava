<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kaishengit.mapper.TopicMapper">

    <select id="findId" resultType="Topic" parameterType="int" resultMap="topicMap">
        <!--用resultMap 这时候要在上面加resultMap=“xx”,这个xx要在下面另建，id要等于xx。-->
        SELECT t_topic.*,t_user.username ,
        t_user.avatar,t_node.nodename

        FROM t_topic INNER JOIN
        t_user on t_user.id=t_topic.userid
        INNER JOIN t_node on t_node.id=t_topic.nodeid
        where t_topic.id=#{id}


        <!--
        用SQL时候的解决办法
       SELECT t_topic.*,t_user.username as `user.username`,
       t_user.avatar as `user.avatar`,t_node.nodename as `node.nodename`
       FROM t_topic INNER JOIN
       t_user on t_user.id=t_topic.userid
       INNER JOIN t_node on t_node.id=t_topic.nodeid
      where t_topic.id=#{id}

      -->
    </select>
    <resultMap id="topicMap" type="Topic">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="text" property="text"/>
        <result column="createtime" property="createtime"/>
        <result column="userid" property="userid"/>
        <result column="nodeid" property="nodeid"/>
        <result column="viewnum" property="viewnum"/>
        <result column="favnum" property="favnum"/>
        <result column="likenum" property="likenum"/>
        <result column="replynum" property="replynum"/>
        <result column="replytime" property="replytime"/>

        <association  column="userid" javaType="User" property="user"  >
            <id column="userid" property="id"/>
            <result column="username" property="username"/>
            <result column="avatar" property="avatar"/>
        </association>

        <association column="nodeid" javaType="Node" property="node" >
            <id column="nodeid" property="id"/>
            <result column="nodename" property="nodename"/>
        </association>
    </resultMap>

</mapper>